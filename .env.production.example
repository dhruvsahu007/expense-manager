# ═══════════════════════════════════════════════════════
# SplitMint — Production Environment Variables
# Copy this file to .env.production and fill in values
# ═══════════════════════════════════════════════════════

# ── PostgreSQL ────────────────────────────────────────
POSTGRES_USER=splitmint
POSTGRES_PASSWORD=CHANGE_ME_strong_random_password_here
POSTGRES_DB=splitmint

# ── Backend ───────────────────────────────────────────
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE_ME_generate_a_64_char_random_string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ── URLs ──────────────────────────────────────────────
# Set to your domain or EC2 public IP
# Examples:
#   http://54.123.45.67         (EC2 IP, no SSL)
#   https://splitmint.com       (custom domain + SSL)
FRONTEND_URL=http://YOUR_EC2_PUBLIC_IP_OR_DOMAIN
NEXT_PUBLIC_API_URL=http://YOUR_EC2_PUBLIC_IP_OR_DOMAIN
